<!DOCTYPE html>
<html>

  <head>
    <title>Aunties</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>


    <div class="navigation navbar navbar-expand-sm navbar-light navbar-auntie">
      <div class="logo">
      <h2><%= link_to "Auntie's", root_path,  class: "brand-font-logo hidden", id: "logo"  %></h2>
      </div>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
        <% if current_user %>
          <li class="nav-item">
            <span class="nav-link">
              <%= current_user.first_name %>
            </span>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, class: "nav-link", method: :delete %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "Registration", new_user_registration_path, class: "nav-link" %>
          </li>
        <% end %>

          <li class="nav-item dropdown">
            <%= cl_image_tag current_user.photo.key, class: "avatar user-avatar" if current_user%>
          </li>
        </ul>
      </div>
    </div>

      <%= yield %>
      <%= render "shared/flashes" %>
  </body>
</html>
