<div class="section-container">
  <h3>Todays Lunches</h3>

<%= link_to "View All Meals", meals_path %>
<%= link_to "My Orders", my_orders_path(current_user) %>

  <section class="tile-view">
    <%= render 'tile' %>
    <%= render 'tile' %>
    <%= render 'tile' %>
  </section>


  <div class="info-box">
  </div>
</div>
